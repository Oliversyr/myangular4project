/**
 * 表格操作中的更多弹窗的位置指令
 * @Created by: yangr 
 * @Created Date: 2017-12-15 15:20:08 
 * @Last Modified by: yangr
 * @Last Modified time: 2017-12-15 15:48:29
 */

/* import {Directive, ElementRef, HostListener, Input, AfterViewInit} from '@angular/core';

@Directive({
    selector: '[sui-grid-more]'
})

export class SuiGridMoreDirective implements AfterViewInit {

    constructor(private el: ElementRef) {
        console.log('this.el.nativeElement.clientHeight==', el.nativeElement.clientHeight);
    }
    

    ngAfterViewInit() {
        console.log('this.el.nativeElement.clientHeight==', this.el.nativeElement.clientHeight);
    }
}

 */